spring:
  application:
    name: api-gateway
server:
  port: 80
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
#zuul:
#  routes:
#    user-consumer: /user-consumer/**
#    user-service: /user-provider/**